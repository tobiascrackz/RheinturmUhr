#neuer mqtt user

# MAC adresse ohne ":" !!!!!, passwort ist auch die mac adresse ohne doppelpunkt

docker exec -it mosquitto mosquitto_passwd /mosquitto/config/pwfile User-246F28B5F3C4

docker restart mosquitto



# neue firmware

export compiled binary. umbennen in fimrware.bin und ins hauptverzeichnis kopieren

signatur erzeigen mit

openssl dgst -sha256 -sign private.pem -out firmware.bin.sig firmware.bin

version.txt anpassen

version.txt. firmware.bin und firmware.bin.sig auf github hochladen


# zertifikate erstellen

openssl genpkey -algorithm RSA -out private.pem -pkeyopt rsa_keygen_bits:2048

openssl rsa -pubout -in private.pem -out public.pem

